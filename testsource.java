BufferedReader reader = new BufferedReader(new FileReader("data/test2.tsv"));
String line = null;
while ((line = reader.readLine()) != null) {
    line = reader.readLine();
    if(line.containsLowerCase(q) && line.containsLowerCase(b))
        if

}
else {
            	
    String[] values = line.split("\t");